const Nav = (props) => {
  return (
    <div>
      <h1>“The best batch in CodeLine is the .NET Full Stack group with {props.title1} and {props.title2}, and {props.title3} complete the greatness of the batch.” {props.title}</h1>
    </div>
  );
};

export default Nav;
